; 节点派发表 · N2 借东风（数据驱动 · S2 生命周期/派发/文案 · mainline-quest §3.2 / architecture §6.2）。
; node_id: n2_east_wind —— 与 S1 节点数值模型 data/nodes/n2_east_wind.tres（rewrite-causality §3.2，待 P5-5）按 node_id 关联。
; 本文件只存 S2 拥有的生命周期/派发/文案字段；**不含** vars/blueprints/verbs/causal_out/delta_critical 等 S1 内部数值态。
;
; MVP 关键决策（issue #16 / game-concept §7.1）：
;   - t_dispatch = ON_CHAPTER_ENTER（章节进入即派发 N2，无前序/存在性门控）—— 与 mainline §3.2 示例的
;     ON_PREDECESSOR_RESOLVED 不同：示例假设完整 N1→N2 链，而 MVP 退化到「章节进入→派 N2→章末」单节点。
;   - prereq_nodes = []（MVP 无前序）；existence_dep_ref 空（N2 是链中节点，自身存在性无依赖）。
;
; 文案基调按「冷峻第三方观测者/记录员」倾向撰写（mainline §1.2/§6.3；game-concept §9① 待审批）。
; system_dispatch_voice 沿用 rewrite-causality §3.2 示例语气。
; ⚠️ quest_reward_mult/quest_cp_flat_bonus 为首版倾向值，待 P2-4(S3 经济曲线) + P6 Playtest 定。
[gd_resource type="Resource" script_class="QuestNodeDispatchData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/foundation/data_resources/quest_node_dispatch_data.gd" id="1_main"]

[resource]
script = ExtResource("1_main")
node_id = &"n2_east_wind"
dynasty = &"dyn_threekingdoms_chibi"
chapter_id = &"ch_chibi_war"
t_dispatch = 0
prereq_nodes = []
existence_dep_ref = &""
target_scene = &"scene_altar"
quest_reward_mult = 1.2
quest_cp_flat_bonus = 10
objective_short = "前往七星坛，决定东风是否借成"
objective_long = "周瑜欲火攻却缺东南风。诸葛亮登坛借风。你可以破坏、截断、策反，或由你之手亲自借这阵风。此节点的成败将决定华容道是否出现。"
system_dispatch_voice = "已锁定目标：借东风。当前节点偏差归零。记录员就位。"
system_complete_voice = "节点已确认。偏差已记录，因果已传递。"
system_vanish_voice = "目标节点未触发存在条件。世界线已重排。"
causal_preview_hint = "借东风成败 → 决定华容道是否出现"
